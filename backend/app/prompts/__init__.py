"""LLM prompt builders.

Prompts live here, not inline in services, so they are easy to find, version, and
extend. Services own orchestration (gateway calls, parsing, validation); this
package owns the words.
"""
