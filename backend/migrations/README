Async Alembic migrations for super-hype. See ../BACKEND.md section 6.

Commands (always via uv):
  uv run alembic revision --autogenerate -m "message"
  uv run alembic upgrade head
  uv run alembic downgrade -1
